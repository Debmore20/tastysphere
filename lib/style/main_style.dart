import 'package:flutter/material.dart';

class AppStyle {
  static const Color transparent = Colors.transparent;
  static const Color colorOne = Color.fromARGB(255, 255, 255, 255);
  static Color colorTwo = Colors.teal;
  static Color colorThree = Colors.teal.shade700;
  static Color colorFour = Colors.teal.shade900;
  static const Color textColorOne = Color.fromARGB(255, 6, 1, 10);

  static const double appbarHeight = 100;
}
